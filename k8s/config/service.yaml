apiVersion: v1
kind: Service
metadata:
  labels:
    domain: blitzshare
  name: file-share-service
  namespace: file-share-ns
  
spec:
  selector:
    app: file-share-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
